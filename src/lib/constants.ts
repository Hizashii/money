/** Max size per PDF file in bytes (10 MB). */
export const MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024;

/** Max size shown in UI (e.g. "10 MB"). */
export const MAX_FILE_SIZE_LABEL = "10 MB";

/** localStorage key for last extraction (session history). */
export const SESSION_STORAGE_KEY = "money_last_extraction";
